{
  "rules": {
    "status": {
      "$roomId": {
        ".read": true,
        "$uid": {
          ".write": "auth != null && auth.uid === $uid",
          ".validate": "!newData.exists() || (newData.hasChildren(['online','name','lastSeen']) && newData.child('name').isString() && newData.child('name').val().length <= 20)"
        }
      }
    }
  }
}
